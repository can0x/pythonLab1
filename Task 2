#Write a Python-script that performs the standard math functions on the data.
#The name of function and data are set on the command line when the script is run.
#The script should be launched like this:
#>>python my_task.py add 1 2

import sys
import math
#0  #1 #2 #3 - sys.argv pos
def add(a, b):
    return a + b
def minus(a, b):
    return a - b
def multiply(a, b):
    return a * b
def divide(a, b):
    return a / b

result = ''
solution = 'result = ' + sys.argv[1] + '(' + sys.argv[2]
for i in range(3, len(sys.argv)):
     solution += ', ' + sys.argv[i]
solution += ')'
exec(solution)
print(result)
